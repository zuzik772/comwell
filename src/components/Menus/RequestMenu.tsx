import React, { FC } from "react";
import { Menu } from "./Menu";

export const RequestMenu: FC = () => {
  return (
    <Menu title={"Pick a place"} name={"request"} children={undefined}></Menu>
  );
};
